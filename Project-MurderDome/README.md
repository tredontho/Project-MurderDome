# Project-MurderDome

Build Dependencies: npm, nodejs, browserify, typescript

Install node dependencies:
```shell
npm install
```

Build:
```shell
npm run-script build
```

Run:
```shell
npm start
```
